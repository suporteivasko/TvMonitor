import { BtnReturn } from "../../Components/Buttons/BtnReturn"

export const AdminLayout = ({ children }) => {
    return(
        <div className="main">
           
            {children}
        </div>
    )
}